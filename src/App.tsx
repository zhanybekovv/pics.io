import React, { FC } from 'react';
import Widgets from './components/Widgets';
import './App.css';

const App: FC = () => {
	return (
		<div className='App'>
			<Widgets />
		</div>
	);
};

export default App;
